export class Ingredient {
  // Its basically creates the fields of the class and assign
  // to them the parameters from the constructor. nice syntax!
  constructor(public name: string, public amount: number) {}
}
