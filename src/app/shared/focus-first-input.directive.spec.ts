import { FocusFirstInputDirective } from './focus-first-input.directive';

describe('FocusFirstInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusFirstInputDirective();
    expect(directive).toBeTruthy();
  });
});
